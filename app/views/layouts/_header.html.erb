
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to " AllPlaces", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to " Places", places_path, class: "fa fa-lg fa-map-marker" %></li>
      </ul>

      <%= form_tag places_path, method: "get", class: "navbar-form navbar-left" do %>

        <div class="form-group">
        <%= text_field_tag :search, params[:search], class: "form-control", placeholder: "Type a place" %>
        </div>
        <%= submit_tag "Search", name: nil, class: "btn btn-default" %>

      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li><%= link_to " Dashboard", dashboard_path, class: "fa fa-id-card-o fa-lg" %></li>
          <li><%= link_to " Settings", edit_user_registration_path, class: "fa fa-cogs fa-lg" %></li>
          <li><%= link_to " Logout", destroy_user_session_path, method: :delete, class: "fa fa-sign-out fa-lg" %></li>
        <% else %>
          <li><%= link_to " Login", new_user_session_path, class: "fa fa-sign-in fa-lg" %></li>
          <li><%= link_to " Signup", new_user_registration_path, class: "fa fa-user-plus fa-lg" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
